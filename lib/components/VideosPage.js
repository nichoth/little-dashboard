var h = require('virtual-dom/h');

module.exports = VideosPage;

function VideosPage(opts) {

  return {};

}

VideosPage.render = function(state) {
  return h('div.videos-page', ['this is videos ']);
};
